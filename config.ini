[database]
# yes = local SQLite demo mode. no = MongoDB mode.
sqlite = yes
sqlite_path = data/grit.sqlite3

[sync]
# When enabled, writes made in SQLite mode are also attempted against MongoDB.
# Keep this as no for company demos when MongoDB access is not available.
dual_write_mongo = no
